#include <iostream>
using namespace std;

int main()
{
    // int a = 2;
    // int b = a+1;

    // if((a=3)==b){
    //     cout<<a;

    // }
    // else
    // {
    //     cout<<a+1;
    // }

    // Problem no. 2
    /*
    int a = 24;
    if(a>20){
        cout<<"Love";
    }
    else if(a == 24){
        cout<<"Lovely";
    }
    else{
        cout<<"Babbar";
    }
    cout<<a;
    */

    // Problem no.3
    /*
    char ch;
    cin >> ch;

    cout << "The ascii value of ch is " << int(ch) << endl;

    if ((int(ch)) >= 48 && (int(ch)) <= 57)
    {
        cout << "The value is between 0 to 9." << endl;
    }
    else if ((int(ch)) >= 65 && (int(ch)) <= 90)
    {
        cout << "The character lies between uppercase latters (between A to Z)." << endl;
    }
    else if (int(ch) >= 97 && int(ch <= 122))
    {
        cout << "The character lies between lowercase latters between a to z." << endl;
    }
    else
    {
        cout <<" The value does not lies between 0-9 nor in uppercase neither in lowercase may be it is a special character. " <<endl;
    }
    */


   
}